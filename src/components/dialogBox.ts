
export enum DialogBoxType {
    CONFIRMATION = 1
}

const appendDialogBox = (targetElement: HTMLElement, type: DialogBoxType) => {
    switch(type) {
        case DialogBoxType.CONFIRMATION: {
            return;
        }
    }
}
